import moment from "jalali-moment";const JALALI_NU_LOCALE = "fa-IR-u-nu-arab";const GREGORIAN_NU_LOCALE = "en-US-u-nu-latn";const PERSIAN_DIGITS = "۰۱۲۳۴۵۶۷۸۹";const formatDayJalali = (day) => {    const jalaliDate = moment(day).locale("fa");    return jalaliDate.format("jD").replace(/\d/g, (d) => PERSIAN_DIGITS[d]);};const formatDayGregorian = (day) => day.getDate().toLocaleString(GREGORIAN_NU_LOCALE);const formatWeekNumberJalali = (weekNumber) => weekNumber.toLocaleString(JALALI_NU_LOCALE);const formatWeekNumberGregorian = (weekNumber) => weekNumber.toLocaleString(GREGORIAN_NU_LOCALE);const formatMonthCaptionJalali = (date) => {    const jalaliDate = moment(date).locale("fa");    const month = jalaliDate.format("jMMMM");    const year = jalaliDate.format("jYYYY").replace(/\d/g, (d) => PERSIAN_DIGITS[d]);    return `${month} ${year}`;};const formatMonthCaptionGregorian = (date) => {    const gregorianDate = moment(date);    const month = gregorianDate.format("MMMM");    const year = gregorianDate.format("YYYY").toLocaleString(GREGORIAN_NU_LOCALE);    return `${month} ${year}`;};export {    formatDayJalali,    formatDayGregorian,    formatWeekNumberJalali,    formatWeekNumberGregorian,    formatMonthCaptionJalali,    formatMonthCaptionGregorian};